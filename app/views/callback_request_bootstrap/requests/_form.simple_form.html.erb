
<div id="div_callback_request">

<span><%=t('callback_form.description')%></span>

<%= simple_form_for @request, remote: true, html: {class: "form-horizontal", role: "form"} do |f| %>
  <%= hidden_field_tag :modal, @modal %>


  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%=t 'callback_form.errors', n: pluralize(@request.errors.count, "error") %></h2>
      <ul>
      <% @request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, t('callback_form.fields.name'), class: "col-sm-2 control-label" %>
    <div class="col-sm-6">
      <%= f.text_field :name, :class=>'form-control', required: true  %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :phone, t('callback_form.fields.phone'), class: "col-sm-2 control-label" %>
    <div class="col-sm-6">
      <%= f.text_field :phone, :class=>'form-control', required: true %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :notes, t('callback_form.fields.notes'), class: "col-sm-2 control-label" %>
    <div class="col-sm-6">
      <%= f.text_area :notes, :class=>'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit t('callback_form.submit'), :class=>'btn btn-primary' %>
    </div>
  </div>

<% end %>

</div>
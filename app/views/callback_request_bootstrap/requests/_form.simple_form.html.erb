
<div id="div_callback_request">

<span><%=t('callback_request.description')%></span>

<%= simple_form_for @request, remote: true, html: {class: "form-horizontal", role: "form"} do |f| %>
  <%= hidden_field_tag :modal, @modal %>
  <%= hidden_field_tag :autoclose, @autoclose %>


  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%=t 'callback_request.errors', n: pluralize(@request.errors.count, "error") %></h2>
      <ul>
      <% @request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= f.label :name, t('callback_request.fields.name'), class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= f.text_field :name, :class=>'form-control', required: true  %>
    </div>
  </div>
  <div class="form-group row">
    <%= f.label :phone, t('callback_request.fields.phone'), class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= f.text_field :phone, :class=>'form-control', required: true %>
    </div>
  </div>
  <div class="form-group row">
    <%= f.label :notes, t('callback_request.fields.notes'), class: "col-sm-3 col-form-label" %>
    <div class="col-sm-9">
      <%= f.text_area :notes, :class=>'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-sm-3">
    </div>
    <div class="col-sm-9">
      <%= f.submit t('callback_request.submit'), :class=>'btn btn-secondary' %>
    </div>
  </div>

<% end %>

</div>